请为我撰写一篇文章的其中一个章节。整体的目录如下：
<%= outline %>

【重要指令】
1. 严格基于参考内容进行写作，所有事实、数据和观点必须来源于提供的参考材料
2. 绝对禁止编造、虚构或推测参考内容中没有明确提到的信息
3. 如果参考内容中没有相关信息，请明确说明"根据现有资料，该方面信息不足"

参考内容如下：
<%= contents %>

<% if defined?(article_style) && article_style %>
文章风格要求：<%= article_style %>
<% end %>
<% if defined?(article_audience) && article_audience %>
目标读者：<%= article_audience %>
<% end %>
本章节的字数要求，不超过：<%= length %> 字

<% if title.include?("章")%>
本章的标题是：<%= title %>
本章的内容简介是：<%= summary %>
注意1：只需要本章的一个引导性的介绍，不要涉及后续的小节。
<% else %>
本节的标题是：<%= title %>
本节的内容简介是：<%= summary %>
注意1： 展开论述本小节的内容，不要涉及后续的小节。
<% end %>

注意2：只输出内容，不输出章节标题

注意3：在章节最后列出所有注释，通过脚注展示引用的资料标题、URL
【脚注格式要求】
- 使用标准的Markdown脚注格式：[^1]、[^2]等
- 在文章末尾使用：[^1]: 资料标题 - URL
- 确保每个引用都有对应的脚注，且编号正确

注意4：严格检查文章内容，确保：
- 所有引用都真实存在于参考内容中
- 不编造资料中不存在的事实或数据
- 不添加参考内容未提及的细节
- 如果参考内容信息不足，请如实说明